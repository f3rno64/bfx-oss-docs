'use strict'

require('./global_options')

/**
 * Parameters passed to the internal stylus Normalizer
 *
 * @memberof module:stylus
 * @typedef {object} NormalizerOptions
 *
 * @property {string} [`hoist atrules`] - unknown
 */
