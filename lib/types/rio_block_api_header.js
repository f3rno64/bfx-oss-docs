'use strict'

/**
 * Readme.IO `[block:api-header]` data structure, as embedded in their markdown
 * export format. Represents an inline header.
 *
 * @typedef {object} module:bfx-oss-docs.RIOBlockAPIHeaderData
 * @property {string} title - title as markdown
 */
